SELECT city, country FROM city
INNER JOIN counntry ON city.country_id = country.country_id;

SELECT payment_id, first_name, last_name FROM payment
INNER JOIN customer ON payment.custoemr_id = customer.customer_id;

SELECT rental_id, first_name, last_name FROM rental
INNER JOIN customer ON rental.custoemr_id = customer.customer_id;
